/**
 * Regression tests -- each file documents a specific bug fix.
 *
 * Naming convention: <module>-<description>.test.ts
 * Example: blend-zero-weight-division.test.ts
 *
 * Each test file header should reference the commit/issue that fixed the bug.
 * The test MUST fail before the fix and pass after.
 *
 * To add a regression test:
 * 1. Write the test that reproduces the bug (it should fail)
 * 2. Fix the bug
 * 3. Verify the test passes
 * 4. Commit both the fix and the regression test together
 */
export {};
