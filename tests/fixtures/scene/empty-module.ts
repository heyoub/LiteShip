/** Fixture for tests/unit/cli/commands/scene-compile.test.ts — exports
 * neither a sceneComposition capsule nor a SceneContract, so scene-compile
 * must short-circuit with the missing-export error. */
export const noise = 42;
