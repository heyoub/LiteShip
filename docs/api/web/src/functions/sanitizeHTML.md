[**LiteShip**](../../../README.md)

***

[LiteShip](../../../modules.md) / [web/src](../README.md) / sanitizeHTML

# Function: sanitizeHTML()

> **sanitizeHTML**(`html`): `string`

Defined in: [web/src/security/html-trust.ts:139](https://github.com/TheFreeBatteryFactory/czap/blob/main/packages/web/src/security/html-trust.ts#L139)

Convenience wrapper that always applies the `sanitized-html` policy.
Use when a caller just wants the "clean the markup" behaviour without
threading options.

## Parameters

### html

`string`

## Returns

`string`
